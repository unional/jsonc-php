{
  "name": "unional/jsonc",
  "description": "jsonc library",
  "type": "library",
  "license": "MIT",
  "authors": [
    {
      "name": "unional",
      "email": "homawong@gmail.com"
    }
  ],
  "scripts": {
    "coverage": "export XDEBUG_MODE=coverage && phpunit --log-junit .php/junit.xml --coverage-clover .php/clover.xml",
    "lint": "phplint",
    "test": "phpunit",
    "verify": "composer lint && composer coverage"
  },
  "minimum-stability": "stable",
  "autoload": {
    "psr-4": {
      "Unional\\Jsonc\\": [
        "php",
        "test"
      ]
    }
  },
  "require-dev": {
    "phpunit/phpunit": "^9.5",
    "overtrue/phplint": "^2.3"
  }
}
